<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            <Label
                Text="Adding an IconTintColorBehavior is fine as long as I haven't set any properties."
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Adding an IconTintColorBehavior is fine as long as I haven't set any properties."
                >
                <Image.Behaviors>
                    <toolkit:IconTintColorBehavior />
                </Image.Behaviors>
            </Image>

            <Label
                Text="When I add an IconTintColorBehavior and set its TintColor, I cannot set the HeightRequest."
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            <Image
                Source="dotnet_bot.png"
                Aspect="AspectFit"
                SemanticProperties.Description="When I add an IconTintColorBehavior and set its TintColor, I cannot set the HeightRequest."
                >
                <Image.Behaviors>
                    <toolkit:IconTintColorBehavior
                        TintColor="Red"
                        />
                </Image.Behaviors>
            </Image>

            <Label
                Text="Setting both TintColor and HeightRequest causes Windows UI to crash."
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Setting both TintColor and HeightRequest causes Windows UI to crash."
                >
                <Image.Behaviors>
                    <toolkit:IconTintColorBehavior
                        TintColor="Red"
                        />
                </Image.Behaviors>
            </Image>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
